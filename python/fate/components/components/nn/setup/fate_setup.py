from fate.components.components.nn.nn_setup import Setup


class FateSetup(Setup):

    def __init__(self, model_config, 
                       dataset_config,
                       optimizer_config
                   ) -> None:
        super().__init__()

    def setup(self):
        pass