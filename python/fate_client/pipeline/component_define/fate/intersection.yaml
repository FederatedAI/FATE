component:
  name: intersection
  description: ''
  provider: fate
  version: 2.0.0.alpha
  labels: []
  roles:
    - guest
    - host
  input_definitions:
    parameters:
      method:
        type: str
        default: raw
        optional: true
    artifacts:
      input_data:
        type: dataset
        optional: false
        stages:
          - default
        roles:
          - guest
          - host
  output_definitions:
    artifacts:
      output_data:
        type: dataset
        optional: false
        stages:
          - default
        roles:
          - guest
          - host
schema_version: v1

