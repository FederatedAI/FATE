component:
  name: reader
  description: ''
  provider: fate
  version: 2.0.0.alpha
  labels: []
  roles:
    - guest
    - host
  input_definitions:
    parameters:
      path:
        type: str
        default: null
        optional: false
  output_definitions:
    artifacts:
      data:
        type: dataset
        optional: false
        stages:
          - default
        roles:
          - guest
          - host
schema_version: v1