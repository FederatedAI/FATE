component:
  name: feature_scale
  description: ''
  provider: fate
  version: 2.0.0.alpha
  labels: []
  roles:
    - guest
    - host
  input_definitions:
    parameters:
      method:
        type: str
        default: standard
        optional: false
    artifacts:
      train_data:
        type: dataset
        optional: false
        stages:
          - train
        roles:
          - guest
          - host
      input_model:
        type: model
        optional: false
        stages:
          - predict
        roles:
          - guest
          - host
      test_data:
        type: dataset
        optional: false
        stages:
          - predict
        roles:
          - guest
          - host
  output_definitions:
    artifacts:
      train_output_data:
        type: dataset
        optional: false
        stages:
          - train
        roles:
          - guest
          - host
      output_model:
        type: model
        optional: false
        stages:
          - train
        roles:
          - guest
          - host
      test_output_data:
        type: dataset
        optional: false
        stages:
          - predict
        roles:
          - guest
          - host
schema_version: v1
